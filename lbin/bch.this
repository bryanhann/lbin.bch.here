#!/bin/sh
cat <<USAGE
The BCH.THIS command suite

    BASE is the path to the nearest superdirectory
    to the current working directory such that the
    subdirectory [BASE/.bch/bch.this/] exists. 

    DEEP is the path to that subdirectory.

COMMANDS:
    bch.this                print this help
    bch.this.base           print BASE to stdout (if found).
    bch.this.deep           print DEEP to stdout (if found).
    bch.this.main ARGS*     run DEEP/main ARGS*
USAGE
